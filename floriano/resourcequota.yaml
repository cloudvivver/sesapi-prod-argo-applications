apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute
  namespace: floriano
spec:
  hard:
    requests.cpu: "4"
    requests.memory: "8Gi"
    limits.cpu: "20"
    limits.memory: "36Gi"
    pods: "10"
