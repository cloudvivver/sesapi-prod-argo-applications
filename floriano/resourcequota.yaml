apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute
  namespace: floriano
spec:
  hard:
    requests.cpu: "5.5"
    requests.memory: "12Gi"
    limits.cpu: "22"
    limits.memory: "40Gi"
    pods: "10"
