apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute
  namespace: saojoao
spec:
  hard:
    requests.cpu: "8"
    requests.memory: "18Gi"  # Temporário para rollout master-71
    limits.cpu: "20"
    limits.memory: "60Gi"  # Temporário para rollout master-71
    pods: "12"
