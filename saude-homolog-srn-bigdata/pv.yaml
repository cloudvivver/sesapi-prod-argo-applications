apiVersion: v1
kind: PersistentVolume
metadata:
  name: azurefile-pv-srnhomolog
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  azureFile:
    secretName: azure-secret
    shareName: stcuidarpython01
    readOnly: false
    secretNamespace: default
