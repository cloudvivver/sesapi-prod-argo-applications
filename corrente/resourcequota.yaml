apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute
  namespace: corrente
spec:
  hard:
    requests.cpu: "8"
    requests.memory: "8Gi"
    limits.cpu: "15"
    limits.memory: "30Gi"
    pods: "15"
