apiVersion: v1
kind: PersistentVolume
metadata:
  name: azurefile-pv-ctv-audio
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  azureFile:
    secretName: azure-secret
    shareName: stcuidarnew01
    readOnly: false
    secretNamespace: default