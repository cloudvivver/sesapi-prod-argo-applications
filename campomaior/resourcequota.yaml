apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute
  namespace: campomaior
spec:
  hard:
    requests.cpu: "8"
    requests.memory: "20Gi"
    limits.cpu: "22"
    limits.memory: "40Gi"
    pods: "12"
