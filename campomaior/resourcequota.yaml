apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute
  namespace: campomaior
spec:
  hard:
    requests.cpu: "5.5"
    requests.memory: "18Gi"  # Temporário para rollout jemalloc (era 12Gi)
    limits.cpu: "22"
    limits.memory: "60Gi"  # Temporário para rollout jemalloc (era 40Gi)
    pods: "10"
