apiVersion: v1
data:
  # Spring Boot Configuration
  SPRING_PROFILES_ACTIVE: "prod"
  SERVER_PORT: "8080"
  
  # JVM Configuration
  JAVA_OPTS: "-Xmx512m -Xms256m -XX:+UseG1GC -XX:+UseContainerSupport -XX:MaxRAMPercentage=75"
  
  # Azure Service Bus Configuration - Using MSI
  SB_NAMESPACE: "cuidar-connect.servicebus.windows.net"
  SB_DEFAULT_QUEUE: "sb_report_queue_dev"
  
  # Logging Configuration
  LOGGING_LEVEL_ROOT: "WARN"
  LOGGING_LEVEL_BR_COM_BIGDATASYS_EVENTPUBLISHER: "DEBUG"
  LOGGING_LEVEL_COM_AZURE: "INFO"
  LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_WEB: "WARN"
  LOGGING_FILE_MAX_SIZE: "10MB"
  LOGGING_FILE_MAX_HISTORY: "7"
  
  # Management Configuration
  MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: "health,info,metrics"
  MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS: "when-authorized"
  
  # Performance Configuration
  SERVER_TOMCAT_MAX_THREADS: "200"
  SERVER_TOMCAT_MIN_SPARE_THREADS: "10"
  SERVER_TOMCAT_CONNECTION_TIMEOUT: "20000"
  
  # Compression Configuration
  SERVER_COMPRESSION_ENABLED: "true"
  SERVER_COMPRESSION_MIME_TYPES: "text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json"
  
  # Security Configuration
  SERVER_SERVLET_SESSION_COOKIE_SECURE: "true"
  SERVER_SERVLET_SESSION_COOKIE_HTTP_ONLY: "true"
  
  # Application Specific Configuration
  APP_NAME: "saude-event-agent"
  APP_VERSION: "1.0.0"
  APP_ENVIRONMENT: "production"

kind: ConfigMap
metadata:
  labels:
    io.kompose.service: saude-event-agent-env
  name: env
  namespace: saude-event-agent