apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute
  namespace: parnaiba
spec:
  hard:
    requests.cpu: "4.5"
    requests.memory: "10Gi"
    limits.cpu: "20"
    limits.memory: "36Gi"
    pods: "10"
