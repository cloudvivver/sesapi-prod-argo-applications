# Gerar senha: openssl rand -base64 24
# kubectl create secret generic postgres-readmodel-secret \
#   --from-literal=password=SUA_SENHA_AQUI \
#   -n saude-bi \
#   --dry-run=client -o yaml > postgres-readmodel-secret.yaml
# Não versionar o arquivo com a senha real; usar Sealed Secrets ou similar em produção.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-readmodel-secret
  namespace: saude-bi
  labels:
    app: postgres-readmodel
type: Opaque
stringData:
  password: "CHANGE_ME"  # Substituir e preferir secret externo (Sealed Secrets / Vault)
