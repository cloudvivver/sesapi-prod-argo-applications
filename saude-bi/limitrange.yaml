apiVersion: v1
kind: LimitRange
metadata:
  name: defaults
  namespace: saude-bi
spec:
  limits:
    - type: Container
      default:
        cpu: "1"
        memory: "2Gi"
      defaultRequest:
        cpu: "200m"
        memory: "512Mi"
