apiVersion: v1
kind: Service
metadata:
  name: cep-api-service
  namespace: edne-endereco
  labels:
    app: cep-api
spec:
  type: ClusterIP
  selector:
    app: cep-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
