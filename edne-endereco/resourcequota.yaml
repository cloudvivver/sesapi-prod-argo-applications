apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute
  namespace: edne-endereco
spec:
  hard:
    requests.cpu: "4"
    requests.memory: "8Gi"
    limits.cpu: "6"
    limits.memory: "12Gi"
    pods: "10"
    persistentvolumeclaims: "2"
